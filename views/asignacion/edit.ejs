<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
	<title>Edit Asignacion</title>
	<link rel="stylesheet" href="https://bootswatch.com/4/minty/bootstrap.min.css">
</head>
<body class="container" style="margin-top: 50px;">

	<% if (messages.error) { %>
	<div class="alert alert-danger" role="alert"><%- messages.error %></div>
	<% } %>

    <div class="card"> 
    	<div class="card-header">
			Edit User
		</div>
        <div class="card-body">
            <form action="/asignacion/update/<%= idasignacion %>" method="post">
                <div class="form-group">
                    <label>Mantenimiento:</label>
                    <select class="custom-select" name="idMantenimientoAsignacion" >
                        <option value="">Seleccione un mantenimeinto:</option>
                        <% for(var i = 0; i< dataMan.length; i++) { %>
                            <option value="<%= dataMan[i].idmantenimiento%>"<% if (dataMan[i].idmantenimiento == idMantenimientoAsignacion ){%>selected <%}%> ><%= dataMan[i].nombreman%></option>
                        <% } %>
                      </select>
                </div>
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-info" value="Update"/>
                </div>
            </form>
        </div>
    </div>

</body>
</html>